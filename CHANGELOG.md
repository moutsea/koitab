# 更新日志

本项目遵循语义化版本:`主版本.次版本.修订号`。
**每次改动都会升级版本号**;功能新增升次版本,问题修复升修订号。

## 5.1.2

**官网上线与自动发布**。

- 官网部署到 Cloudflare Workers,支持 koitab.com 与 www.koitab.com
- GitHub main 分支更新后自动构建和部署;发布前运行扩展回归测试,同步更新日志与下载包
- 固定构建 Node.js 版本,提供统一的 Worker 构建与部署命令

## 5.1.1

**修复标签与收藏的数据安全问题**,统一浏览器兼容要求与官网资源生成。

- 去重保留完整 URL 的 hash、路径和查询参数,不同路由不再误关;归档分别保存这些地址
- 收藏搜索后按确认过的条目 ID 删除,保留未命中的收藏;删除从最新存储读取,避免过期界面覆盖新收藏
- 归档读取失败或数据格式异常时立即中止,不覆盖旧收藏、不关闭标签;写入失败同样保留页面
- 归档关闭每个标签前重新读取状态,地址、窗口、访问时间发生变化或已激活、固定、正在导航的页面保留不关
- 整理、归档和删除共用执行锁,执行期间禁用相关按钮、范围和阈值设置,刷新界面不会解除锁
- 最低 Chrome / Edge 版本统一为 121,与存储 Promise 和 lastAccessed 所需 API 一致
- Logo 生成脚本同时更新当前 Next.js 官网和旧版静态页资源
- 新增扩展数据安全与并发操作回归测试,可运行 `node --test tests/*.test.cjs`

## 5.1.0

官网迁移到 Next.js(App Router + SSG),部署到 Cloudflare Workers,SEO 基建补齐。
扩展本体不受影响(仍是免构建纯 JS)。

- 新增 `nextjs/` 独立应用;页面文案、JSON-LD、RSS 共用一份数据源(`app/content.ts`),
  避免"页面改了、结构化数据没改"的漂移
- **SEO**:每个路由都预渲染为完整静态 HTML(`○ Static`);`metadata` 补齐
  title / description / canonical / Open Graph / Twitter Card / keywords;
  JSON-LD 三类结构化数据(WebSite、SoftwareApplication、**FAQPage** 与页面问答同源);
  自动生成 `/sitemap.xml` 与 `/robots.txt`
- **RSS**:新增 `/feed.xml`(RSS 2.0,构建期静态生成),内容解析自 `CHANGELOG.md`;
  新增 `/changelog` 页承载全部版本记录,feed 每条指向对应锚点;
  首页新增「最近更新」区块,让内容随版本保持新鲜
  (说明:RSS 是分发渠道而非 SEO 手段 —— SEO 靠可抓取的静态 HTML 与结构化数据)
- 下载包改由 `tools/build-site.py` 产出到 `nextjs/public/downloads/`,页面直接下载;
  同一脚本把 CHANGELOG 解析成 `releases.json`
- 部署方式改为 **Cloudflare Workers**(OpenNext 适配器):`npm run deploy` =
  `opennextjs-cloudflare build` + deploy;`wrangler.jsonc` 内置 koitab.com 与
  www.koitab.com 的自定义域路由
- 依赖锁定:Next 16.3.5 + React 19.2.8(create-next-app@16.3.5 的 recommended 组合)
  + @opennextjs/cloudflare 1.20.6 + wrangler 4.132.0
- **坑位记录**:外层 shell 若带 `NODE_ENV=development`,`next build` 会在预渲染
  `/_global-error` 时崩在 `useContext of null`(vercel/next.js#97046);
  构建脚本已强制 `NODE_ENV=production` 并写明原因
- 旧的纯静态 `landing/` 内容已全量迁移,暂时保留作对照,确认后可删

## 5.0.1

官方下载上线:koitab.com 直接下载扩展包。

- landing 页顶部与页尾的下载按钮指向 `downloads/koitab-latest.zip`,点击即下载(带 download 属性,不会被当成导航)
- 新增 `tools/build-site.py`:一条命令产出可部署的 `dist/`(landing 静态站 + 扩展 zip);
  zip 顶层为 `koitab/` 文件夹,解压后直接「加载已解压的扩展程序」,并同步产出按版本号命名的 `koitab-v{版本}.zip`
- 发布包**排除开发用文件**(debug.html / debug.js —— 那是我们自查 lastAccessed 用的诊断页,不分发给用户)
- 「三步开始使用」文案同步:选择解压出的 `koitab` 文件夹(原文案写的 `extension` 与发布包结构不符)
- 部署流程见 README「发布与部署(Cloudflare Pages)」

## 5.0.0

**归档改存 KoiTab 自己的收藏夹,与浏览器书签彻底解耦**(用户澄清:归档 ≠ 写浏览器书签)。

- 归档不再调用 `chrome.bookmarks` 写任何东西:收藏条目存进
  `chrome.storage.local`(`koiArchived`:url/title/domain/archivedAt/唯一 id),
  **先落盘、后关页**;storage 写失败 → 一个页都不关(收藏式关闭的底线)
- 这一刀顺带根治了整条"书签环境病":实测踩过的「search 不返回文件夹」
  「其他书签容器 id '2' 不存在」全是书签树的兼容坑 —— 现在与 KoiTab 无关了
- 「收藏夹」页两个分区:**KoiTab 收藏**(实心「藏」印;打开/删单条/按站点清空,只动自己的存储)
  与 **浏览器书签**(白文「签」印,**只读**:可搜索可打开,但没有任何删除/写入口;
  书签 API 不可用时该分区自动消失,收藏分区照常)
- 同 URL 幂等:隔几天再开到同一页再归档,收藏只留一份;"存了没关掉"的残局再点只补关
- 保留 `bookmarks` 权限,仅用于只读展示浏览器书签分区
- 预览/汇总/tooltip 文案改口「KoiTab 收藏」;归档与收藏测试按新模型重写
  (存储断言、全程零书签写调用断言、双分区与只读断言)

## 4.0.4

定位并修复用户实测报错「Can't find parent bookmark for id.」:**根文件夹的父容器 id 不能硬编码**。

- 此前建「KoiTab 归档」根文件夹写死 `parentId:'2'`(标准 Chrome 的「其他书签」惯例 id)。
  这台机器的书签树里没有 id 为 '2' 的节点 —— Edge / 企业策略 / 分叉 Chromium 的容器 id
  不保证,于是建根直接抛错、全部书签建不成
- 现在用 `bookmarkParentCandidates()` 按优先级找容器并**逐个试到成功**:
  惯例 id '2' → 标题匹配(其他书签/其他收藏夹/Other Bookmarks/Favorites)→ 根下末位/首位子文件夹
- 所有候选都被拒时,如实报「找不到可写书签的容器文件夹」,且出于安全一个页也不关
- 测试新增:无 '2' 节点的 Edge 式书签树可正常归档;容器全拒收时页原样保留 + toast 带真实错误

## 4.0.3

「书签一条都没建成」时,把 **Chrome 的原话错误**放进提示,不再让用户猜。

- 4.0.2 的提示「多半是书签权限没生效」是我的猜测,把排查责任推回给了用户 —— 现在
  `archiveCore` 捕获每一步(找根/建根/扫夹/建夹/写书签/关页)的**第一个真实错误**,
  汇总句变成 `书签一条都没建成(原因:<Chrome 原文>) —— 出于安全一个页也没关`
- `chrome.bookmarks` 整体不可用时单独预判,提示指向重新加载扩展
- `planArchive` 不再把「空 URL(导航中)」的标签当归档候选 —— 这种页根本存不进书签,
  收了必然失败

## 4.0.2

修复**归档点了没反应**:预览明明写着 66 条,点下去页却一个不关。

- **主因:批量 `chrome.tabs.remove([66 个 id])` 是整批成败** —— 只要其中一个标签
  恰好在这几秒里被别的途径关掉(或在未保存的分组里),整批一起被拒,
  65 个能关的也陪葬;而书签早已逐条建好,toast 又是一句「没有需要归档的 🎉」
  —— 看起来就完全"没生效"。改为**逐个关闭**,一个坏 id 只影响它自己
- **幂等**:归档前扫描「KoiTab 归档」现有子夹与已归档 URL ——
  书签夹按站点名复用、同一 URL 不重复建。上一轮"书签建了页没关"的残局,
  再点一次**只补关**,不会产生第二份书签
- **去掉 `window.confirm`**:弹窗里它可能被失焦无声吞掉(点了等于取消),而预览行
  早已把数字写得明明白白;按钮改为执行期显示「归档中…」,不再是毫无反馈
- 汇总提示改讲事实:`书签已收进…,但页没关掉 —— 再点一次只补关` /
  `把 N 个…并关闭 · K 条早前已在书签里,没重复建 · F 个没动(页都还在)`
- 注:若你上次点击其实已经建出了书签(去书签管理器看「KoiTab 归档」),
  新版第一次点击会识别它们并复用,只把页补关,不会重复

## 4.0.1

修复"没有需要归档的标签页 🎉"把三种不同情况糊成一句庆祝的问题 —— 归档没候选时,
现在**如实告诉你是哪一种**,下一步该干什么:

- **读不到使用时间**(一个标签的 `lastAccessed` 都拿不到 → Chrome/Edge < 121,或刚重启时间被刷):
  明说需要 Chrome 121+,并指向 `debug.html` 核对原始数据 —— 这是能力问题,不是"没有旧页"
- **读得到、但都没闲够**:报出"最久的一个 X 前还用过",并**主动给一个刚好能命中的更短档**
  (如切到 3 天档);若连 3 天档都不到,则老实说"再晾几天就有",不做瞎建议
- **能归的都是正在看/固定的**:直说"开着的页都在你眼前",这种才不标警
- 三种解释同时出现在:归档预览行(朱色提示)、按钮 tooltip、点按后的 toast;
  文案与按钮置灰共用同一份判断(describeArchiveEmpty),不会说法不一
- 另:诊断指标行、比例条、整理预览均不受影响

## 4.0.0

「操作」页重构为**诊断页**:先看体检报告,再决定整不整理。

- 打开这一页只有一枚朱印引导卡 +「开始诊断」按钮 —— **不再一上来就摊四个功能卡**
- 点「开始诊断」自动统计当前所有窗口:重复网页、跨窗口待集中的同站点标签、未分组散标签、
  单标签分组、位置交错的散标签、N 天没用的标签,每行一根**比例条**(占全部标签页的百分比,
  需要处理的是朱砂色,可观察的是墨色)
- 报告下方是**带预览的两个行动卡**:
  - 「一键整理」:动手前就写明结果 —— 新合并出几个分组、关闭几个重复、
    收拢几个散标签、取消几个空分组(数字来自真实规划器,不是文案)
  - 「归档未用标签」:写明几条书签收进「KoiTab 归档」、关闭几个页、几个站点、
    重复 URL 只记几份
- **分组折叠不再是独立卡片** —— 并入整理卡作为一行小设置(超过几个自动收起);
  整理范围同样收进整理卡
- 整理/归档完成后报告**原地刷新**(重复归零、按钮自动置灰),随时可「重新诊断」
- 数字口径与按钮状态共用同一份 computeMetrics(),报告、预览、按钮三者永远一致;
  新增诊断页测试 27 项(含 fake windows.create 搬移种子标签的真实语义校验)

## 3.5.1

回滚 3.5.0 的误改:**logo 还是那幅手绘锦鲤**,只是底板从白色换成宣纸色。

- 3.5.0 把图标整个重绘成了程序画的印章字 —— 那不是"换个背景色",越界了,已撤销
  (`apply-seal-logo.py` 与印章图标全部作废删除,`apply-logo.py` 从手绘原图重新生成全套图标与
  landing 的 logo / favicon)
- `apply-logo.py` 新增 `PAPER_PLATE` 常量:圆角底板由白色改为宣纸色 `#f5f0e4`,与水墨界面同调;
  墨迹、红笔尖、构图一个像素没动
- 弹窗题头恢复"册页装裱"样式(去掉旋转贴印效果)

## 3.5.0

logo 重绘为**朱砂印章**,与水墨界面同一套语言。

- 新增 `tools/apply-seal-logo.py`:直接生成「印泥朱砂底 + 米白纸色『鲤』+ 内圈白文边线」
  的圆角印面,一次产出扩展图标四件套(16/32/48/128)、landing 的 logo(512)与 favicon(64)。
  本机若无楷体则回退宋体 Black(碑刻感);脚本自带像素级自检(圆角透明/边线/印面/笔画)
- 弹窗题头的 logo 从「白底小画 + 装裱框」改为**一枚微旋(-3°)钤印**,加印泥色投影;
  和「归 / 签 / 整」那些分区小印同族
- 旧手绘风并未丢失:`assets/logo-source.jpg` 与 `tools/apply-logo.py` 原样保留,
  重跑 `python3 tools/apply-logo.py` 即可还原旧图标
- landing 与 favicon 同步替换为新印面

## 3.4.0

收藏夹内区分「KoiTab 归档的」和「你自己书签栏里的」—— 加了两级总分区。

- 收藏夹现在是两个**虚拟大分区**:「KoiTab 归档」(实心朱印「归」标记)与「浏览器书签」
  (白文印「签」标记,包裹书签栏及其子夹 —— 即"书签栏外层再加一个总文件夹"的效果)
- 分区标题行:箭头 + 印章 + 名称 + 「N 夹 · M 条」计数,下方压一道朱线划层级;
  **点分区标题整块折叠**,单独记住状态(`@section:` 前缀键,与文件夹偏好互不干扰)
- 归档分区内的组名**剥掉「KoiTab 归档/」前缀**(显示 `github.com` 而非
  `KoiTab 归档/github.com`)—— 区标题已经说明归属,不再重复
- 「全部折叠 / 全部展开」升级为分区粒度;搜索时命中所在分区强制展开
- 这是**界面层的虚拟分区**:不新建、不移动、不重排你真实的书签树

## 3.3.0

收藏夹的文件夹现在可以**折叠 / 展开**了。

- 点夹子标题(完整路径那一行)收起整个文件夹,只留「路径名 + 条数」;再点恢复
  —— 箭头、点按区域、吸顶标题都与「标签页」页的站点分组同款同感
- 折叠状态**按路径记住**(存 `koiFavCollapsed`,与站点分组的偏好分开、互不串台),
  关掉弹窗再开还是你上次折叠的样子;书签挪走后失效的路径记录会被顺手清理
- 与标签页同规则的两条细节:**搜索时强制展开全部**(命中结果不能被折叠藏住),
  此时底栏「全部折叠」置灰;清空搜索后恢复各自的折叠偏好
- 底栏新增「**全部折叠 / 全部展开**」批量按钮,文案随状态切换
- 默认仍是全部展开 —— 收藏的是你自己的书签,不该像标签页那样自动收起

## 3.2.1

- 去掉标题栏右上角的「鲤」朱印;版本号徽标移到它原来的位置(最右)
- 卡片上的「整/归/叠」小印不受影响

## 3.2.0

收藏夹升级为**浏览器书签的实时全量视图** —— 搜索从此搜得到书签内容。

- 你问的「一键导入书签」不需要导入这一步:收藏夹和书签的数据本来就在同一个地方
  (`chrome.bookmarks`),导一份副本只会过期。现在收藏 tab **直接读整棵书签树**:
  你的普通书签和「KoiTab 归档」同列展示,搜索框同时覆盖两者
- 分组规则:每条书签归到它**直接的父文件夹**名下,组标签显示完整路径
  (如「KoiTab 归档/github.com」「工作/项目」;容器「书签栏/其他书签」不掺进前缀)。
  归档组排在最前 —— 找回归档页仍是高频动线,普通夹紧随其后
- **安全边界:清空整夹的 × 只给归档组** —— 用户自己的真实书签夹不会被一键清空;
  单条删除对所有书签可用,提示写明 Chrome 的 Ctrl+Shift+Z 可撤销
- 页脚统计相应改为「共 N 条书签 · M 个文件夹」;空态文案更新
- 仍是懒加载:不进收藏 tab 完全不碰书签

## 3.1.1

移除「操作」页里的「还会做什么」速览卡 —— 它只是文字复述,占掉首屏一屏半,
真正用得上的三个操作卡(整理 / 归档 / 折叠)往上提了。

## 3.1.0

弹窗整体重绘为**中国水墨书签风**。

- **宣纸底**:米黄纸面 + 极淡墨晕 + 一层帘纹质感;卡片改「册页」——小方角、
  界格细线、纸白底,不再是大圆角现代卡片
- **朱砂印章点缀**:标题栏一枚微旋的「鲤」压角章;四张功能卡的图标换成白文小印
  「整 / 归 / 叠 / 览」;「一键整理」主按钮 = 一方印泥红大印;当前标签行是朱砂晕 + 界尺线
- **字体换楷/宋衬线**(系统自带楷体做标题与控件、宋体做正文,**零网络字体、离线可用**);
  tab 导航是三枚题签,选中的朱砂题头 + 底部朱线;分段控件选中态做成「拓片」(黑底纸字)
- **站点色点从彩虹 hsl 换成十色国画色**(黛蓝/青黛/松绿/赭石/朱砂/绛紫/藤黄/花青/茶褐/月白),
  圆点改为菱形「圈点」;同一站点仍然恒定一色。Chrome 原生分组颜色不变(那是浏览器自己的语言)
- 表情符号从导航与图标位全部退场(彩色 emoji 会破坏水墨版面);toast 改墨牌样式;
  深色模式重绘为「砚池夜色」
- 折叠缩进、悬停显隐 ×、吸顶标题这三条行为/样式锚点原样保留,10 个套件全绿

## 3.0.2

修复「收藏夹一直显示读取书签失败」。

- **根因**:找「KoiTab 归档」根文件夹用的是 `chrome.bookmarks.search({title})` ——
  它在部分 Chrome 版本里**不返回文件夹**(按标题搜的只有书签)。收藏夹与归档都
  依赖这一步,一旦搜不到就出错/误判。现在改为遍历 `getTree()` 找根文件夹,
  一次调用连子树一起拿回,不依赖 search 的行为
- 连带修复:同一坑会让**每次归档都新建一个重复的「KoiTab 归档」根文件夹**
  (search 搜不到已有文件夹 → 误判"没有" → 再建一个)。测试桩现在刻意模拟
  "search 不返回文件夹"的真实行为,并锁定"两次归档只建一个根文件夹"
- 失败提示现在会带上**具体错误原因**(如权限未生效、API 不可用),而不是干巴巴一句
  "读取书签失败";书签 API 完全不可用时明确提示"到 chrome://extensions 重新加载"
- 收藏夹读取逻辑全程只读,不会改任何书签

## 3.0.1

修复整理时散标签被"按打开时间"重排的问题 —— **顺序不该在用户没要求时改变**。

- 把散标签收进新窗口时,此前按 `tab.id`(≈ 打开时间)排序,会造成两种"找不到":
  两个窗口的散标签**互相穿插**、用户**拖拽调整过的顺序被抹回原样**
- 现在按**用户看到的顺序**搬运:当前窗口的散标签整块在前,其余窗口按窗口编号整块跟随
  (窗口之间不交错);同一窗口内按标签栏从左到右的位置(index)排,
  而不是按 id —— 拖拽过的位置才是他认的顺序
- 「标签页」页里的重排步骤本来就保持散标签相对顺序,不受影响

## 3.0.0

**弹窗重构为三个 tab**,整体观感重做。

- **⚡ 操作**:所有功能与配置集中在这一个页面 ——
  「一键整理」卡(含整理范围切换)、归档卡(含 3/7/30 天阈值)、分组折叠卡
  (**新增可配置**:自动折叠阈值 5/8/12/从不,默认 8);还有一张"还会做什么"的能力速览
- **🗂 标签页**:原来的主体内容(搜索 + 站点分组列表 + 全部折叠),搜索框从顶栏挪到了这一页
- **⭐ 收藏夹**:归档走的页都能在这找回 —— 直接读「其他书签 / KoiTab 归档」子树,
  按站点分组;点一条 = 在当前窗口开新标签(**书签保留**,收藏的语义是不删);
  悬停 × 删单条;组头「全部打开」/「清空」;支持按标题或网址过滤
- 三个 tab 带**数量徽标**(标签页常显;收藏数读过书签才显示),**停留的 tab 会被记住**
- 收藏夹**懒加载**:不进这个 tab 就完全不碰书签,其他页零开销
- 视觉翻新:卡片化布局、渐变主按钮、带指示条的 tab 导航、收藏分组用与站点分组一致的样式,
  深色模式全部适配
- 所有配置(范围 / 归档阈值 / 折叠阈值 / 当前 tab)都存在本地 storage,零上传

安全边界不变:归档仍是"先收藏、后关闭";每个窗口的激活标签与固定标签永不归档;
书签没建成就绝不关页。

## 2.2.0

新增「归档未用标签」:**先收藏、再关闭**,不是直接丢掉。

- 弹窗新增一行次级操作:阈值下拉(3 / 7 / 30 天,默认 7 天,选择会记住)+「归档未用标签」按钮
- 超过阈值没用的标签页,先按站点收进书签「其他书签 / KoiTab 归档 / 站点名 /」,然后才关闭;
  书签里带着原页面的标题和地址,想找随时能打开 —— 不存在"关了就找不到"
- **宁可漏、不可误杀**:
  - 每个窗口正在浏览的标签页、固定标签页**永不归档**(也因此任何窗口都不会被归档到关闭);
  - 拿不到 `lastAccessed` 的一律跳过(Chrome 121 以下没有这个字段);
  - 浏览器内部页(chrome:// 等)不收录 —— 存书签没有意义;
  - 同一个 URL 开着多份时,书签只记一份、标签页全部关闭,书签里不会留重复噪音
- **失败保护**:书签没建成的标签页绝不关闭。建根文件夹失败就一条不写、一个不关;
  某个站点建夹失败只影响该站点,其余照常
- 关闭前会弹一次确认框,写明要归档几个、去哪儿;归档后因此只剩 1 个成员的分组自动取消
- 归档遵循顶部的「整理范围」:「本窗口」只归档当前窗口,「所有窗口」才动全部窗口
- 权限新增 `bookmarks`(仅此一项;仍然免构建、无第三方依赖)

## 2.1.2

新增开发用诊断页 `debug.html`(只读,不影响任何整理行为),用来确认
「清理长期未用标签」这个功能是否可行。

- 背景:`tabs.Tab.lastAccessed`(Chrome 121+)能拿到标签页**最后一次被访问**的时间。
  但有一个未确认项:**浏览器重启后时间戳是保留还是重置**。
  若被重置,恢复的上次会话标签页会全部变成"刚打开",「3 天未用」就永远判不出来
- 诊断页回答三件事:能不能拿到 `lastAccessed`、哪些标签页是 `undefined`、
  以及**重启前后时间戳是否保留**(保存快照 → 重启浏览器 → 回到页面看对比结论)
- 结论判定抽成纯函数 `verdictFor()`,并有测试覆盖"保留 / 被重置 / 数据不足 / 不误判"
  四种情况 —— 判错会得出错误的技术结论,所以必须测
- 本页只读:不会关闭、移动或激活任何标签页

## 2.1.1

界面优化:折叠后的分组不再显示标签页信息。

- **折叠后只留域名**:此前折叠时会露出该站点第一个标签页的标题(以及「等 N 个」),
  现在整组收起,只剩标题栏上的「站点名 + 数量」。全部折叠时,弹窗里就是一排干净的站点,
  不再夹着某一条标签页的信息
- **关闭整组的 × 改为悬停出现**:折叠后的界面更干净,同时也避免误触这个不可撤销的操作
  (单个标签页的 × 一直是这个行为)
- 顺带移除预览行相关的一整套 DOM 渲染与样式(`.koi-preview-row` / `.koi-preview-more`),
  折叠时不再需要"隐藏普通行、保留预览行"那套特例规则,改为整组隐藏

## 2.1.0

整理的目标终态改成**两个明确的窗口**,不再"分组进新窗口、散标签原地不动"。

- **当前窗口 = 分组的展示窗口**:所有窗口里同一站点的标签一律收进当前窗口并成组
  (此前是收进新建的窗口)。这样当前窗口只收不搬、永远不会被搬空,
  "窗口被关掉导致弹窗消失、整理中断在半路"的风险彻底不存在 —— 不再需要 2.0.1 里
  "当前窗口全是候选标签时把它当目标"那类特例
- **新窗口 = 散标签的收纳窗口**:构不成分组的散标签(站点合计只有 1 个)统一收进新窗口。
  此前散标签留在各自的窗口里,结果整理完还是好几个窗口
- **其他窗口被搬空后关闭**:只剩固定标签的窗口会留下来(固定标签不搬动)
- **散标签已经全在同一个窗口里时不再搬动**,所以重复点「一键整理」不会有任何多余动作,
  窗口也不会越点越多
- **修复:固定标签不再被卷入分组**。Chrome 不允许固定标签进入分组,
  而按 (窗口, 站点) 分桶时把固定标签算了进去 —— 一旦它的站点和分组同域,
  整个桶的成组都会失败(或产生非法分组)。现在固定标签完全不参与分桶与成组
- **当前窗口里没有站点可成组、也没有固定标签时保持原样**:把它腾空只会让窗口被关掉、
  弹窗中断,所以这种情况下按钮直接置灰

## 2.0.1

修复 2.0.0 里"分组没有全部集中到一个窗口"的两个原因。

- **修复:各自"已经很整齐"的分组不会被合并**。此前只要某个站点已经在同一个窗口、
  同一个分组里,就被判定为"无需处理"而跳过 —— 于是两个窗口里各有一个分好组的分组时,
  它们双双被跳过,分组永远留在不同窗口里,不会合并。现在只要候选站点**分散在多个窗口**,
  就一定会被集中到一起;"已经整齐就跳过"只在候选标签**已经全在同一个窗口**时生效
  (这时只需补上还没成组的,不会有任何多余搬运,重复点整理也不会有动作)
- **调整:当前窗口全是可整理标签时,改为把"当前窗口"当作集中目标**,不再新建窗口。
  此前是把激活标签所在站点留在当前窗口(锚点),结果当前窗口只剩那一个分组 ——
  看起来就是"出现了一个只有一个分组的窗口"。现在所有分组都会集中到当前窗口里,
  而且当前窗口在这种情况下**只收不搬**,永远不会被搬空,弹窗中断的风险彻底消除
- 提示文案会说明分组集中到了哪个窗口(新窗口 / 当前窗口 / 就地成组),
  以及本次会关闭几个被搬空的窗口

## 2.0.0

**整理方式改变**:不再只是就地归类,而是把所有窗口的同站点标签集中进一个**新窗口**,
让你原来的窗口只剩散标签。这是一次交互上的重做,所以主版本号跳到 2.0.0。

核心变化:

- **跨窗口集中**:点「一键整理」后,所有窗口里同一站点的标签会被收进一个**新建的窗口**
  并按站点成组 —— 你原来的窗口保持干净,只留下不参与集中的散标签
- **归并标准改为跨窗口统计**:某站点在所有窗口合计 ≥2 个标签就会集中
  (此前是"每个窗口内各自 ≥2 个",所以"一个窗口 1 个"的同站点标签不会被合并)
- **固定标签留在原窗口**:通常是邮箱 / 日历这类常驻页,不参与搬运
- **被搬空的窗口会关闭**:原窗口的标签全被搬走时,Chrome 会关掉空窗口——
  标签不会丢,它们都在新窗口里;提示里会说明关闭了几个窗口

一条必须保留的例外:

- **当前窗口不能被搬空**。弹窗依附于当前窗口,窗口一旦被关掉弹窗就消失,
  整理会中断在半路(标签已搬走却还没成组)。所以当当前窗口的标签**全是**候选标签时,
  会把「激活标签所在站点」留作**锚点**,它的标签集中到当前窗口并就地成组。
  代价是这一组不在新窗口里,提示里会说明。只有 1 个候选站点时,也就不会新建窗口。

顺序与幂等:

- 步骤顺序:去重 → 取消孤儿分组 → 集中到新窗口并成组 → 收拾原窗口 → 重排
- 取消孤儿分组放在集中**之前**:只剩 1 个标签的分组被取消后,它的标签就能参与集中
- 集中的搬运必须在成组之前:Chrome 的分组无法跨窗口,先集中才能成组
- **已整理好的站点不会被反复搬动**:同站点标签已在同一个窗口、同一个分组、且分组里
  没有别的站点时直接跳过。否则每点一次整理都会把它搬去一个"新窗口",窗口越点越多
- 新窗口以 `focused: false` 创建,不抢焦点,弹窗也不会因失焦被关闭
- 「本窗口」范围下不新建窗口、不做任何跨窗口搬运,仍是原地成组

## 1.4.1

修复:同一站点的不同子域没有被合并到一起。

- **按「可注册域名」归并**:此前只去掉 `www.` 后取完整主机名,导致
  `zhuanlan.zhihu.com`(知乎专栏)与 `www.zhihu.com`(知乎主站)被当成两个站点,
  专栏的标签页进不了知乎分组。现在两者都归为 `zhihu.com`
- 同理受益:`m.weibo.cn`、`blog.csdn.net`、`pan.baidu.com` / `tieba.baidu.com`、
  `mail.qq.com`、`gist.github.com` 等子域都会与各自站点合并
- **不会过度归并**:扩展拿不到公共后缀列表(PSL),所以用「最后两段 + 常见多段后缀兜底表」。
  `a.com.cn` 与 `b.com.cn`、`foo.co.uk` 与 `bar.co.uk` 仍然是不同站点;
  `user1.github.io` 与 `user2.github.io`、`x.blogspot.com` 这类私有后缀也各自独立
- 副作用(需留意):同一主域下的不同子域现在会合并成一组,
  例如 `docs.google.com` 与 `mail.google.com` 都会归入 `google.com`。

## 1.4.0

新增「散标签另开新窗口」:整理后当前窗口只留分组标签。

- **当前窗口只保留分组标签**,未分组的散标签自动挪到一个**新建的窗口**
- **只拆「当前窗口」**:即使切到「所有窗口」范围,也只拆你正在用的这个窗口,
  其他窗口仅做常规整理(分组靠左、取消孤儿分组),不会一次冒出好几个新窗口
- **固定标签留在原窗口**:通常是邮箱/日历这类常驻页,搬到新窗口很别扭
- **安全底线:当前窗口没有分组就不拆**。否则把散标签全搬走会让原窗口一个标签都不剩,
  而 Chrome 会直接关掉空窗口 —— 那不是整理,是丢窗口
- **不抢焦点**(`focused: false`):一是不把你从当前窗口拽走,二是弹窗一旦失焦就会被关闭,
  那样你就看不到整理结果了
- 散标签在新窗口中**保持原有先后顺序**;作为一键整理的第 5 步,
  排在「取消孤儿分组」之后(刚被取消分组而变成散标签的也一起搬走)、「重排」之前
  (散标签都要搬走了,先重排纯属白做)
- 已整理过的窗口不会重复新建窗口(原窗口已经没有散标签),窗口数不会越点越多

## 1.3.0

新增「取消孤儿分组」,并修正整理幂等性。

### 新增

- **自动取消只剩 1 个标签的分组**:分组的价值在于把多个标签收在一起,只剩 1 个标签的
  分组已经没有意义(还白占一个分组位、挡住"分组靠左"的效果),整理时自动取消。
  空分组会被 Chrome 自己回收,所以只需处理剩 1 个的
- 「取消孤儿分组」作为一键整理的第 4 步,排在**分组之后**:先让分组把标签收齐,
  再清理"收不齐、只剩一个"的残余。否则可能把马上就能凑够 2 个的分组提前拆掉

### 修复

- **整理不再把已经正确的分组拆掉重建**。此前 `合并同类项` 会无条件调用
  `chrome.tabs.group()` 重建所有站点分组,即使它们已经分好了,导致:
  - 每次整理都会把全部分组拆掉重建(分组 id 每次都变)
  - 「一键整理」按钮**永远不会**变成"已经很整齐了",提示还一直声称"合并 N 个站点"
  现在只有当同一站点的标签**还没被收进同一个分组**时才处理;
  用户手动把多个站点混放于一个分组时,仍会按站点拆开(行为与之前一致)
- 按钮的置灰判断与悬停提示同步修正,并新增"取消 N 个单标签分组"一项

## 1.2.0

新增「重排」步骤:整理时把分组和未分组的标签页分开摆放。

- **分组靠左、未分组靠右**:同一个窗口里,属于分组的标签页彼此挨着排在左边,
  未分组的标签页统一排到它们右边,不再交错
- **绝不打乱分组**:只移动「未分组且未固定」的标签页,并且一律追加到窗口末尾。
  因此同一分组内成员的顺序、分组与分组之间的先后顺序都完整保留
- **为什么不用「把分组挪到左边」的做法**:Chrome 要求同一分组的标签页在标签栏里连续。
  移动分组内的标签页,中途可能把分组拆开,或让标签落进别的分组区间而被"吸"进那个分组。
  只把未分组标签追加到末尾,则永远不会落到任何分组区间内部
- **固定标签页不动**:Chrome 要求固定标签只能在最左侧,它们本来就在分组左边
- 重排作为「一键整理」的第 4 步,在分组之后执行(分组完成后才知道哪些标签属于分组);
  已经排好的窗口不会产生任何多余操作
- 整理提示会汇报结果,例如「已 2 个标签页归入 1 个站点分组 · 把 1 个未分组标签页排到分组右侧」

## 1.1.1

界面细节优化。

- **标签页缩进**:分组内的标签页向左缩进 12px,让标签明显从属于上方站点标题。
  原先标签行的小方块起始位置(16px)比站点圆点(22px)还靠左,层级看起来是平铺的;
  缩进后小方块落到圆点右侧,「站点 → 标签页」的从属关系一眼可见
- 折叠时的预览行同样缩进,与普通标签行对齐
- Landing Page 的弹窗示意图同步这一缩进

## 1.1.0

新增「标签页太多」时的浏览体验优化,并统一弹窗排版。

### 新增

- **分组折叠**:点击站点分组标题即可折叠 / 展开,折叠后只留「站点名 + 数量」一行
- **折叠预览**:折叠时额外显示该站点的第一个标签页标题(点击可直接跳转),
  并用「等 N 个」提示总数,不用展开也知道里面有什么
- **自动折叠大分组**:未被用户操作过、且标签数达到 8 个的站点,打开弹窗时默认收起,
  首屏更短、更好扫视;用户手动展开过的不再被自动折叠覆盖
- **偏好模型**:折叠状态从「折叠集合」升级为「明确偏好」(collapsed / expanded),
  用户的每次操作都会被记住,自动折叠永不覆盖用户的选择
- 底栏「全部折叠 / 全部展开」会为所有站点写入明确偏好,一次点击即可全部展开(含大分组)
- 分组标题**吸顶**,长分组滚动时始终能看到当前站点

### 修复 / 优化

- **排版优化**:分组标题原先只有 12px 而标签名 13px,层级倒挂;现统一为 12.5px,
  标题加粗并使用正文色、标签名常规字重,层级关系更清楚;分组之间新增细分隔线
- 折叠与预览共用一套 CSS 可见性规则:折叠时隐藏普通行、保留预览行
- 兼容 1.0.0 的 `koiCollapsed` 旧字段,自动迁移为偏好表,不丢用户已有设置

## 1.0.0

首个版本。

### 新增

- **一键整理**:单个按钮依次执行「关闭重复网页 → 跨窗口聚合 → 合并同类项」,
  每步之间重新查询标签列表,避免基于过期数据操作
- **关闭重复网页**:按「去 hash 的完整 URL」判定重复,保留优先级为
  固定 > 正在浏览 > 更早打开;跨窗口去重时不会把某个窗口的最后一个标签关掉
- **合并同类项**:按 `(窗口, 站点)` 把同类标签页合入浏览器原生分组,
  同一站点在各窗口使用同一颜色
- **跨窗口聚合**:仅在「所有窗口」范围执行,把同类标签搬到该站点标签最多的窗口;
  固定标签不搬动,也不会把任何窗口搬空
- **整理范围切换**:「本窗口 / 所有窗口」,选择会被记住
- **标签页列表**:按站点分组、搜索过滤、点击跳转、单条关闭、整站关闭
- 弹窗界面适配浏览器深色皮肤;图标为白底圆角 + 黑墨手绘 logo
